language: python
python: 3.5
env:
  - TOX_ENV=py34
  - TOX_ENV=py35
  - TOX_ENV=docs
  - TOX_ENV=flake8
install:
  - pip install tox coveralls
script:
  - tox -e $TOX_ENV
after_success:
  - coveralls
notifications:
  irc: "chat.freenode.net#asphalt"
